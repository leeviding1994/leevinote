# 同步约定

客户端采用本地优先策略。用户操作先写入本地存储，手动同步时再与服务器交换数据。

## 状态

- `local`：仅存在于本地，使用 POST 创建。
- `synced`：本地与服务器一致。
- `modified`：已同步记录在本地被修改，使用 PUT 更新。
- `deleted`：本地删除标记，服务器删除成功后物理清理。

## 顺序

1. 推送本地删除。
2. 推送本地新增和修改。
3. 拉取服务器数据。
4. 删除服务器已不存在且本地状态为 `synced` 的记录。
5. 合并远端数据，但不得覆盖 `local`、`modified` 或 `deleted` 状态。

## 冲突边界

当前实现优先保护未同步的本地修改，但尚未提供完整的多设备版本合并。后续应为服务端实体增加版本号，并使用乐观锁检测并发修改。

## 模块差异

- 健康记录当前仅保存在本机，不参与同步。
- 设置同步使用即时 PUT。
- 其他业务模块由用户手动触发同步。
