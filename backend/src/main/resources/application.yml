server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: leevinote-backend

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/leevinote}
    username: ${DB_USERNAME:leevinote}
    password: ${DB_PASSWORD:leevinote123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:false}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  jackson:
    property-naming-strategy: SNAKE_CASE
    default-property-inclusion: NON_NULL

app:
  upload:
    dir: ${UPLOAD_DIR:${user.home}/leevinote/uploads}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:8080}

jwt:
  # Local default only. Production must set JWT_SECRET (>= 32 bytes; recommend 64+).
  secret: ${JWT_SECRET:local-dev-only-change-me-leevinote-jwt-secret-32b-min}
  expiration: ${JWT_EXPIRATION_SECONDS:86400}

logging:
  level:
    com.leevinote: DEBUG
    org.springframework.security: INFO
